#' @param maxWidthLabels Numeric(1). Default NULL. Maximum width of the legend labels in number of characters. 
#' If the width of the legend labels are longer, they are shortened. Set to NULL to not shorten labels.


#' @param plotChr Character(1) or NULL. Default NULL. The name of the chromosome for which the visualization should be done. Must be a valid chromosome name.
#' If set to NULL, other parameters (such as\code{regions}) determine which genomic region should be plotted.
